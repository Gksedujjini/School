@model Edujinni.Models.Student.AcademicSyallabus
@{
    Layout = "~/Views/Shared/_StudentLayout.cshtml";
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>AcademicSyallabus</title>
    <style>
        #drop1{

        }
    </style>
</head>
<body>
    @{
        List<SelectListItem> list = new List<SelectListItem>();
    }
    <div class="col-md-12 mt-4"  style="background-color:#E7E7E7; border-radius:10px;">
        <div class="row mt-4 justify-content-center">
            <div class="col-md-3 mt-2">
                @Html.DropDownListFor(Model => Model.classs, list, "Subjects", new { @class = "form-control",@id="drop1" })
            </div>
            <div class="col-md-3 mt-2">
                @Html.DropDownListFor(Model => Model.units, list, "Units", new { @class = "form-control", @id = "drop1" })
            </div>
        </div>
        <div class="row mt-3 ml-3">
            <div class="col-md-12">
                //DATA IS SHOWN IN GRID VIEW
                //DATA IS SHOWN IN GRID VIEW//DATA IS SHOWN IN GRID VIEW
                //DATA IS SHOWN IN GRID VIEW
                //DATA IS SHOWN IN GRID VIEW//DATA IS SHOWN IN GRID VIEW
            </div>
        </div>
    </div>
        <br />
</body>
</html>
